name: Deploy Astro to GitHub Pages

on:
  # Trigger the workflow every time you push to the `main` branch
  # Using a different branch name? Replace `main` with your branch’s name
  push:
    branches: [develop]
  # Allows you to run this workflow manually from the Actions tab on GitHub.
  workflow_dispatch:

# Allow this job to clone the repo and create a page deployment
permissions:
  contents: read
  pages: write
  id-token: write

#env:
  #PUBLIC_WP_REST_API: ${{ vars.PUBLIC_WP_REST_API }}
  #PUBLIC_WP_USERNAME: bob
  #PUBLIC_WP_PASSWORD: le-testeur
  #PUBLIC_BASE_URL: my-astro-test
  #PUBLIC_SITE_URL: https://cms.albanblanchard.fr/

jobs:
  build:
    env:
      THE_TEST: ${{ secrets.THE_TEST }}
    runs-on: ubuntu-latest
    steps:
      - name: Glop glop
        run: echo "Hello $THE_TEST !"
